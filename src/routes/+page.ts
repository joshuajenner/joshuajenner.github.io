// import type { PostDisplay } from '$lib/types'

// export async function load({ fetch }) {
//     const response = await fetch('blog/api')
//     const posts: PostDisplay[] = await response.json()
//     return { posts }
// }